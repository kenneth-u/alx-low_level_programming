#include "main.h"

/**
 * add - adds two integers and returns the result.
 *
 * @x: number one.
 * @y:number two.
 *
 * Return: Add number one and number two.
 */
int add(int x, int y)
{
	return (a + b);
}
